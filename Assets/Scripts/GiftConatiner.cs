using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GiftConatiner : MonoBehaviour
{
  // Start is called before the first frame update

  void Start()
  {
    // GameObject candy = GameObject.Find("candy");
    // candy.GetComponent<Animator>().;
  }

  // Update is called once per frame
  void Update()
  {

  }
}
